# [백엔드] 고정지출 기본값 수정

> API: `FixedExpenseUpdate` · Phase 4 · 기능번호 4.4  
> 인덱스: [README.md](../README.md)

## 메타

| 항목 | 내용 |
|------|------|
| Phase | 4 (가계부·고정지출) |
| 기능 이름 | 고정지출 기본값 수정 |
| 기능번호 | 4.4 |
| API 이름 | `FixedExpenseUpdate` |
| Method | `PATCH` |
| URL | `/api/v1/fixed-expenses/{fixedExpenseId}` |
| 권한 | 로그인 |
| Content-Type | `application/json` |

## 요청

### Path Variables

| 이름 | 타입 | 필수 | 설명 |
|------|------|:----:|------|
| `fixedExpenseId` | number | ✅ | 고정지출 PK |

### Body

| 필드 | 타입 | 필수 | 설명 |
|------|------|:----:|------|
| `name` | string | ❌ | 이름 |
| `paymentMethodId` | number | ❌ | 수단 |
| `amount` | number | ❌ | **기본** 금액 |
| `paymentDayOfMonth` | number | ❌ | 1~31 |
| `content` | string | ❌ | 내용 |
| `expendGroupId` | number | ❌ | 유형 |
| `startYearMonth` | string | ❌ | 시작 연월 |
| `endYearMonth` | string | ❌ | 종료 연월 |

### Headers

| 이름 | 필수 | 설명 |
|------|:----:|------|
| `Authorization` | ✅ | `Bearer <accessToken>` |
| `Content-Type` | ✅ | `application/json` |

## 응답

### 성공 (`resCode: 200`) — `data`

갱신된 FixedExpense

### 실패 — 대표 `resCode`

| resCode | 조건 |
|---------|------|
| 1001 | 로그인 필요 |
| 3402 | 고정지출 없음 |
| 3401 | 기간 검증 실패 |

## 예시

### 성공

**Request**

```http
PATCH /api/v1/fixed-expenses/1 HTTP/1.1
Host: localhost:8081
Authorization: Bearer eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...
Content-Type: application/json

{
  "amount": 520000
}
```

**Response**

```json
{
  "resCode": 200,
  "data": {
    "fixedExpenseId": 1,
    "name": "월세",
    "paymentMethodId": 1,
    "amount": 520000,
    "paymentDayOfMonth": 5,
    "content": "원룸 월세",
    "expendGroupId": 2,
    "startYearMonth": "2026-01",
    "endYearMonth": "2026-12"
  }
}
```

### 실패

```json
{
  "resCode": 3402,
  "data": {
    "message": "고정지출을 찾을 수 없습니다"
  }
}
```

## 비고

- **고정지출 관리(설정) 행**을 고친다.
- **자동 반영 범위**: 이미 만들어진 월별 고정지출 내역 중 **아직 오지 않은 달**(현재 연월 초과)이면서 **직접 수정되지 않은**(`modified=false`) 행만 새 값을 따라간다. 직접 수정된 달과 지난 달·이번 달 내역은 **그대로 남는다**.
- 지난 달·이번 달까지 새 값으로 맞추려면 [4.9 `FixedExpenseMonthlySync`](./4.9-FixedExpenseMonthlySync.md)를 그 연·월로 호출한다.
- 특정 한 달만 다른 값을 쓰려면 [4.6 `FixedExpenseMonthlyUpdate`](./4.6-FixedExpenseMonthlyUpdate.md)를 쓴다.
