# 2.2. 기능명세 — 프론트엔드

> 기준 문서: [1.요구사항 정리.md](./1.요구사항%20정리.md) · [1.2. 요구사항 - 프론트엔드.md](./1.2.%20요구사항%20-%20프론트엔드.md)  
> 관련: [2.1. 기능명세 - 백엔드.md](./2.1.%20기능명세%20-%20백엔드.md) · [2.기능명세.md](./2.기능명세.md)  
> 대상 모듈: `app-mod/money-app` (Thymeleaf)  
> Base URL: `http://localhost:8080` · 백엔드 API: `http://localhost:8081/api/v1`

## 공통

- DB 직접 접근 없음. 모든 데이터는 **백엔드 API** (`/api/v1/*`)로 조회·변경한다.
- 로그인 API 응답의 토큰을 저장한다. API 호출 시 `Authorization: Bearer`. **로그인/관리자 API는 서버가 토큰 만료·유효성을 검증**한다 (공통 필터). 앱 기동·보호 화면 진입 시 `MemberTokenValidate`로 유효성 확인. **1001/1006** 시 로그인 화면으로 이동 (다른 곳 로그인 안내). Access 만료(`1001`) 시 `MemberTokenRefresh` 시도 후 실패하면 로그인.
- API 실패 시 `resCode`·메시지를 화면(alert 또는 폼 상단)에 표시한다.
- **POST** API 호출 시 입력값은 **Request Body(JSON/multipart)** 로만 전송한다. Query·Path 파라미터 사용하지 않는다.
- **GET** API 호출 시 **Body 없음**. Path **또는** Query **중 하나만** 사용한다 (혼용 금지). **예외:** `StatisticsMonthlyGet`만 Path 연·월 + Query `view?`(`saved`\|`live`) 허용.
- **페이징 목록** (`AdminMemberList`, `FixedExpenseList` 등): Query `offset`·`limit`으로 요청. 응답의 `offset`·`limit`·`totalCount`로 현재 페이지·전체 페이지 수를 계산한다 ([기능명세/_공통.md § 페이징](./기능명세/_공통.md#공유-타입--페이징)).
- **PATCH**: Path(대상 키) + Body(변경 데이터). 변경한 필드만 Body에 담는다. **PUT은 사용하지 않음.**
- **DELETE**: Path(대상 키)만. Body·Query 없음.
- 미로그인 시 보호 URL → `/auth/login` redirect.
- **일반 회원**에게 회원 관리 메뉴·URL 비노출. 관리자 URL 직접 접근 → `/error/forbidden`.
- **본인 가계부 화면만** 제공 (타인 가계부 없음).

**작성 규칙:** 등록·목록·상세 조회·수정·삭제는 **기능마다 한 행**으로 적는다.

### 레이아웃

| 레이아웃 | 용도 | 템플릿 |
|----------|------|--------|
| auth | 로그인·가입·찾기 | `layout/auth.html` |
| main | 로그인 후 (사이드바·상단바) | `layout/main.html` |

### 내부 구조 (구현 가이드)

```
money-app
├── controller/PageController     # GET 페이지 (와이어프레임)
├── web/*WebController            # (추가) POST 폼 → API Body → redirect
└── client/BackendApiClient       # :8081/api/v1 호출, POST는 JSON/multipart body
```

---

### ① 회원 관리

| 기능 | 화면 URL | 템플릿 | 호출 API | 프론트엔드 처리 |
|------|----------|--------|----------|----------------|
| 회원가입 | `GET /auth/signup` | `auth/signup.html` | `MemberSignup` | 입력 폼·비밀번호 규칙 안내·**폰은 하이픈 없이 숫자만**·성공 시 로그인 redirect |
| 로그인 | `GET /auth/login` | `auth/login.html` | `MemberLogin` | id·pw 폼·성공 시 토큰 저장·`/ledger` redirect |
| 토큰 검증 | (앱 기동·보호 화면) | — | `MemberTokenValidate` | 저장 토큰 유효성 확인. 실패 시 로그인 |
| 로그아웃 | (상단 링크) | — | `MemberTokenRevoke` | JWT Token 비활성화 API 후 `/auth/login` |
| id 찾기 | `GET /auth/find-id` | `auth/find-id.html` | `MemberFindId` | 이메일 입력·조회 id 표시 |
| 비밀번호 찾기 | `GET /auth/find-password` | `auth/find-password.html` | `MemberFindPassword` | id·닉네임 확인. 성공 시 변경 단계 표시 |
| 비밀번호 변경 | `GET /auth/find-password` (동일 화면 2단계) 또는 `GET /auth/reset-password` | `auth/find-password.html` 등 | `MemberResetPassword` | 새 pw·확인 입력 후 변경 |
| 본인 정보 조회 | `GET /member/profile` | `member/profile.html` | `MemberGetMe` | 페이지 로드 시 Model 주입, id readonly |
| 본인 정보 수정 | `GET /member/profile` | `member/profile.html` | `MemberUpdateMe` | 저장 버튼 → API → 메시지. **폰은 하이픈 없이 숫자만** |
| 회원 목록 | `GET /admin/members` | `member/admin-list.html` | `AdminMemberList` | **관리자만** 메뉴·검색·페이징 UI (`offset`/`limit`/`totalCount`) |
| 회원 추가 | `GET /admin/members/new` | `member/admin-form.html` | `AdminMemberCreate` | **관리자만** 등록 폼. **폰은 하이픈 없이 숫자만** |
| 회원 상세·수정 | `GET /admin/members/{id}/edit` | `member/admin-form.html` | `AdminMemberGet`, `AdminMemberUpdate` | **관리자만**. **폰은 하이픈 없이 숫자만** |
| 관리자 회원 정지 | `GET /admin/members` | `member/admin-list.html` | `AdminMemberDeactivate` | 확인 후 버튼 |
| 권한 없는 접근 | `GET /error/forbidden` | `error/forbidden.html` | — | Admin API 403 시 redirect |

**비밀번호 안내:** 8자 이상, 영·대·소·특수·숫자 중 3종류 이상

---

### ② 지출수단·소득수단

| 기능 | 화면 URL | 템플릿 | 호출 API | 프론트엔드 처리 |
|------|----------|--------|----------|----------------|
| 등록 | `GET /payments/new` | `payment/form.html` | `PaymentMethodCreate` | 카드 선택 시 유효기간 입력란 표시 |
| 지출·소득 수단 전체 목록 | `GET /payments` | `payment/list.html` | `PaymentMethodList` | 삭제된 수단 포함 테이블 |
| 상세 조회 | `GET /payments/{id}` | `payment/detail.html` | `PaymentMethodGet` | 읽기 전용 |
| 수정 | `GET /payments/{id}/edit` | `payment/form.html` | `PaymentMethodGet`, `PaymentMethodUpdate` | 폼 pre-fill |
| 삭제 | `GET /payments`, `.../detail` | — | `PaymentMethodDelete` | 확인 + 「과거 내역 수단명 유지」 안내 |
| 사용 중인 지출·소득 수단 목록 | `GET /ledger/expenses/new`, `.../edit` | `ledger/expense-form.html` | `PaymentMethodListActive` (`/active/EXPENSE`) | 지출 등록 select 옵션 |
| 사용 중인 지출·소득 수단 목록 | `GET /ledger/incomes/new`, `.../edit` | `ledger/income-form.html` | `PaymentMethodListActive` (`/active/INCOME`) | 소득 등록 select 옵션 |

---

### ③ 지출유형

| 기능 | 화면 URL | 템플릿 | 호출 API | 프론트엔드 처리 |
|------|----------|--------|----------|----------------|
| 등록 | `GET /expend-groups/new` | `expend-group/form.html` | `ExpendGroupCreate` | 아이콘 파일·미리보기 |
| 목록 | `GET /expend-groups` | `expend-group/list.html` | `ExpendGroupList` | 아이콘 `<img>` |
| 상세 조회 | `GET /expend-groups/{id}` | `expend-group/detail.html` | `ExpendGroupGet` | 읽기 전용 |
| 수정 | `GET /expend-groups/{id}/edit` | `expend-group/form.html` | `ExpendGroupGet`, `ExpendGroupUpdate` | |
| 삭제 | 목록·상세 | — | `ExpendGroupDelete` | 실패 시 **비활성화 안내** 알림 |
| 사용 중인 지출유형 목록 | `GET /ledger/expenses/new`, `.../edit` | `ledger/expense-form.html` | `ExpendGroupListActive` | **1개 필수** select, 미사용 제외 |
| 아이콘 표시 | `GET /ledger`, `GET /statistics` | `ledger/monthly.html` 등 | `ExpendGroupIconGet` | img src |

---

### ④ 가계부 (지출·소득·고정지출)

#### 4-1. 지출·소득

| 기능 | 화면 URL | 템플릿 | 호출 API | 프론트엔드 처리 |
|------|----------|--------|----------|----------------|
| 지출 등록 | `GET /ledger/expenses/new` | `ledger/expense-form.html` | `ExpenseCreate` 또는 `ExpenseCreateInstallment` | 일시불/할부 toggle, 할부 시 추가 필드 |
| 지출 상세·수정 | `GET /ledger/expenses/{id}/edit` | `ledger/expense-form.html` | `ExpenseGet`, `ExpenseUpdate` | 할부 **개월·시작월 변경 불가** 안내 |
| 지출 삭제 | `GET /ledger` 등 | — | `ExpenseDelete` | 확인 dialog |
| 소득 등록 | `GET /ledger/incomes/new` | `ledger/income-form.html` | `IncomeCreate` | |
| 소득 수정 | `GET /ledger/incomes/{id}/edit` | `ledger/income-form.html` | `IncomeGet`, `IncomeUpdate` | |
| 소득 삭제 | `GET /ledger` 등 | — | `IncomeDelete` | 확인 |
| 할부 중도상환 | `GET /ledger` | `ledger/monthly.html` | `ExpenseSettleInstallmentRemainder` | 할부 행 버튼 (`PATCH`) |

#### 4-2. 월별 가계부 목록

| 기능 | 화면 URL | 템플릿 | 호출 API | 프론트엔드 처리 |
|------|----------|--------|----------|----------------|
| 월 선택 | `GET /ledger` | `ledger/monthly.html` | `LedgerMonthlyList` | 연·월 select (기본: 이번 달) |
| 월별 가계부 목록 | `GET /ledger` | `ledger/monthly.html` | `LedgerMonthlyList` | 한 테이블, 구분 뱃지 |
| 필터 | `GET /ledger` | `ledger/monthly.html` | `LedgerMonthlyList` | 수단·유형·날짜·keyword |
| 정렬 | `GET /ledger` | `ledger/monthly.html` | `LedgerMonthlyList` | sort·order 버튼 |
| 합계 | `GET /ledger` | `ledger/monthly.html` | `LedgerMonthlyList` | 상단 카드 `expenseTotal`, `incomeTotal` |

**뱃지:** 지출(빨강) / 소득(초록) / 고정(보라) / 할부(주황)

#### 4-3. 지출·소득 엑셀 일괄 등록

| 기능 | 화면 URL | 템플릿 | 호출 API | 프론트엔드 처리 |
|------|----------|--------|----------|----------------|
| 지출·소득 엑셀 양식 받기 | `GET /ledger/excel` | `ledger/excel-upload.html` | `ExpenseIncomeExcelTemplateDownload` | 다운로드 링크/버튼 |
| 지출·소득 엑셀 업로드 | `GET /ledger/excel` | `ledger/excel-upload.html` | `ExpenseIncomeExcelUpload` | `.xlsx` only, **300행 초과 업로드 전 경고** |
| 결과 표시 | `GET /ledger/excel` | `ledger/excel-upload.html` | `ExpenseIncomeExcelUpload` | 성공 건수 또는 행·항목 오류 |
| 중복 | — | — | — | 별도 경고 없음 |

#### 4-4. 고정지출

| 기능 | 화면 URL | 템플릿 | 호출 API | 프론트엔드 처리 |
|------|----------|--------|----------|----------------|
| 등록 | `GET /fixed-expenses/new` | `fixed-expense/form.html` | `FixedExpenseCreate` | 시작/종료 연월 입력 |
| 고정지출 설정 목록 | `GET /fixed-expenses` | `fixed-expense/list.html` | `FixedExpenseList` | 가계부와 **별도 화면**, 페이징 (`offset`/`limit`/`totalCount`) |
| 상세 조회 | `GET /fixed-expenses/{id}` | `fixed-expense/detail.html` | `FixedExpenseGet` | |
| 기본값 수정 | `GET /fixed-expenses/{id}/edit` | `fixed-expense/form.html` | `FixedExpenseGet`, `FixedExpenseUpdate` | 「월별 미수정 달 반영」 안내 |
| 월별 수정 | `GET /fixed-expenses/{id}/monthly` | `fixed-expense/monthly-override.html` | `FixedExpenseMonthlyOverrideGet`, `FixedExpenseMonthlyOverrideUpsert` | 연·월 선택 |
| 삭제 | 목록·상세 | — | `FixedExpenseDelete` | 확인 |
| 가계부 표시 | `GET /ledger` | `ledger/monthly.html` | `LedgerMonthlyList` | 기간 내 자동 포함 (별도 API 없음) |

---

### ⑤ 지출유형 목표금액

| 기능 | 화면 URL | 템플릿 | 호출 API | 프론트엔드 처리 |
|------|----------|--------|----------|----------------|
| 목표금액 목록 | `GET /targets/default` | `target/default.html` | `ExpendTargetList` | `year`+`month` 필수(예: 이번 달). `defaultTargetAmount`·`monthlyTargetAmount` 병기. 사용 중만. 페이징·이름 검색 |
| 목표금액 상세 | `GET /targets/default` | `target/default.html` | `ExpendTargetGet` | 수정·확인 시 단건. 목록과 동일 필드 |
| 기본 목표금액 등록 | `GET /targets/default` | `target/default.html` | `ExpendTargetDefaultUpsert` | `defaultTargetAmount` 입력 |
| 기본 목표금액 수정 | `GET /targets/default` | `target/default.html` | `ExpendTargetDefaultUpsert` | 기본 컬럼 저장 |
| 월별 목표금액 목록 | `GET /statistics` | `statistics/monthly.html` | `ExpendTargetList` | 동일 List API (`year`+`month`). 이중 금액 필드. 페이징·이름 검색 |
| 월별 목표금액 등록 | `GET /statistics` | `statistics/monthly.html` | `ExpendTargetMonthlyUpsert` | `monthlyTargetAmount` inline input |
| 월별 목표금액 수정 | `GET /statistics` | `statistics/monthly.html` | `ExpendTargetMonthlyUpsert` | 그달만 덮어쓰기 |

**미사용 유형:** 목표 목록 API에 포함되지 않음. 사용 여부 변경은 지출유형 등록·수정 화면

---

### ⑥ 월별 통계

| 기능 | 화면 URL | 템플릿 | 호출 API | 프론트엔드 처리 |
|------|----------|--------|----------|----------------|
| 월 선택 | `GET /statistics` | `statistics/monthly.html` | `StatisticsMonthlyGet` | 연·월 select → Path `/monthly/{year}/{month}` |
| 주별 지출 | `GET /statistics` | `statistics/monthly.html` | `StatisticsMonthlyGet` | 표·차트 영역 |
| 지출유형별 | `GET /statistics` | `statistics/monthly.html` | `StatisticsMonthlyGet` | 0원 유형 미표시 |
| 목표 대비 | `GET /statistics` | `statistics/monthly.html` | `StatisticsMonthlyGet` | %·초과/적정/절약 색상 |
| 수단별 | `GET /statistics` | `statistics/monthly.html` | `StatisticsMonthlyGet` | 0원 수단 포함 |
| 고정 vs 일반 | `GET /statistics` | `statistics/monthly.html` | `StatisticsMonthlyGet` | 비율·차트 |
| 월 소득 | `GET /statistics` | `statistics/monthly.html` | `StatisticsMonthlyGet` | `incomeTotal` |
| 통계 저장 | `GET /statistics` | `statistics/monthly.html` | `StatisticsMonthlySave` | 「통계 저장」 버튼 → Body에 `year`, `month` JSON 전송 |
| 통계 조회 | `GET /statistics` | `statistics/monthly.html` | `StatisticsMonthlyGet` | 기본(저장본 우선). `source` 배지 |
| 최신 계산 보기 | `GET /statistics` | `statistics/monthly.html` | `StatisticsMonthlyGet` | `?view=live` — 저장본 무시 재계산. DB 미변경 |
| 저장본 보기 | `GET /statistics` | `statistics/monthly.html` | `StatisticsMonthlyGet` | `view` 생략 — Save한 스냅샷 확인 |
| 갱신 안내 | `GET /statistics` | `statistics/monthly.html` | — | 저장본≠최신일 때 안내. 스냅샷 반영은 「통계 저장」 |

---

## 사이드바 메뉴 ↔ 화면

| 메뉴 | 화면 URL | activeMenu |
|------|----------|------------|
| 월별 목록 | `/ledger` | `ledger` |
| 지출 등록 | `/ledger/expenses/new` | `expense-new` |
| 소득 등록 | `/ledger/incomes/new` | `income-new` |
| 지출·소득 엑셀 일괄 등록 | `/ledger/excel` | `excel` |
| 고정지출 설정 목록 | `/fixed-expenses` | `fixed-list` |
| 고정지출 등록 | `/fixed-expenses/new` | `fixed-new` |
| 지출·소득 수단 | `/payments` | `payments` |
| 지출유형 | `/expend-groups` | `expend-groups` |
| 목표금액 (기본) | `/targets/default` | `targets` |
| 월별 통계 | `/statistics` | `statistics` |
| 본인 정보 수정 | `/member/profile` | `profile` |
| 회원 관리 (관리자) | `/admin/members` | `admin-members` |

---

## 화면 URL 전체 목록

| 화면 | Method | URL | 템플릿 |
|------|--------|-----|--------|
| 루트 | GET | `/` → redirect `/ledger` | — |
| 로그인 | GET | `/auth/login` | `auth/login.html` |
| 회원가입 | GET | `/auth/signup` | `auth/signup.html` |
| 아이디 찾기 | GET | `/auth/find-id` | `auth/find-id.html` |
| 비밀번호 찾기 | GET | `/auth/find-password` | `auth/find-password.html` |
| 비밀번호 변경 | GET | `/auth/reset-password` | `auth/reset-password.html` (또는 find-password 2단계) |
| 권한 없음 | GET | `/error/forbidden` | `error/forbidden.html` |
| 월별 가계부 | GET | `/ledger` | `ledger/monthly.html` |
| 지출 등록 | GET | `/ledger/expenses/new` | `ledger/expense-form.html` |
| 지출 수정 | GET | `/ledger/expenses/{id}/edit` | `ledger/expense-form.html` |
| 소득 등록 | GET | `/ledger/incomes/new` | `ledger/income-form.html` |
| 소득 수정 | GET | `/ledger/incomes/{id}/edit` | `ledger/income-form.html` |
| 지출·소득 엑셀 등록 | GET | `/ledger/excel` | `ledger/excel-upload.html` |
| 고정지출 설정 목록 | GET | `/fixed-expenses` | `fixed-expense/list.html` |
| 고정지출 등록 | GET | `/fixed-expenses/new` | `fixed-expense/form.html` |
| 고정지출 상세 | GET | `/fixed-expenses/{id}` | `fixed-expense/detail.html` |
| 고정지출 수정 | GET | `/fixed-expenses/{id}/edit` | `fixed-expense/form.html` |
| 고정지출 월별 수정 | GET | `/fixed-expenses/{id}/monthly` | `fixed-expense/monthly-override.html` |
| 수단 목록 | GET | `/payments` | `payment/list.html` |
| 수단 등록 | GET | `/payments/new` | `payment/form.html` |
| 수단 상세 | GET | `/payments/{id}` | `payment/detail.html` |
| 수단 수정 | GET | `/payments/{id}/edit` | `payment/form.html` |
| 지출유형 목록 | GET | `/expend-groups` | `expend-group/list.html` |
| 지출유형 등록 | GET | `/expend-groups/new` | `expend-group/form.html` |
| 지출유형 상세 | GET | `/expend-groups/{id}` | `expend-group/detail.html` |
| 지출유형 수정 | GET | `/expend-groups/{id}/edit` | `expend-group/form.html` |
| 기본 목표금액 | GET | `/targets/default` | `target/default.html` |
| 월별 통계 | GET | `/statistics` | `statistics/monthly.html` |
| 본인 정보 | GET | `/member/profile` | `member/profile.html` |
| 회원 목록 | GET | `/admin/members` | `member/admin-list.html` |
| 회원 추가 | GET | `/admin/members/new` | `member/admin-form.html` |
| 회원 수정 | GET | `/admin/members/{id}/edit` | `member/admin-form.html` |

---

## 이후 버전 (명세 제외)

- OAuth·소셜 로그인 화면
- 관리자 타인 가계부 화면
- 할부 중도상환 전용 화면
