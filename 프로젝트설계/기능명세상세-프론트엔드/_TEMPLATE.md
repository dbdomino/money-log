# [프론트] {화면이름}

> 화면: `{ScreenId}` · Phase {phase} · 화면번호 {order}  
> 인덱스: [README.md](./README.md)

> **필드·요소 설명:** 표의 설명 칸은 **그 칸만 보고 의미가 읽혀야** 한다.  
> `동일`·`위와 같음`·`공통 참고`만 적는 것은 **누락**. [_공통.md § 필드 설명 작성 규칙](./_공통.md#필드-설명-작성-규칙-누락)

## 메타

| 항목 | 내용 |
|------|------|
| Phase | {phase} |
| 화면 이름 | {화면이름} |
| 화면번호 | {order} |
| 화면 ID | `{ScreenId}` |
| 표시방식 | `페이지` / `모달` |
| Method | `GET` |
| URL | `/...` (모달이면 부모 + `?m=` · `id` 등) |
| 부모 페이지 | (모달만) 화면번호·URL |
| 템플릿 | 페이지: `{path}.html` / 모달: `{path}/fragments/...` |
| 레이아웃 | `auth` / `main` (모달은 부모와 동일) |
| 권한 | 없음 / 로그인 / 관리자 |
| activeMenu | `{menu-key}` / — |

## 목적

- (이 화면이 사용자에게 제공하는 일 1~2문장)

## 진입

| 항목 | 내용 |
|------|------|
| 진입 경로 | 사이드바·버튼·redirect 등 |
| 미로그인 | `/auth/login` redirect / 해당 없음 |
| 권한 없음 | `/error/forbidden` / 해당 없음 |

## 화면 구성

### 영역

| 영역 | 설명 |
|------|------|

### 입력·표시 요소

| 요소 | 타입 | 필수 | 설명 |
|------|------|:----:|------|

## 호출 API

페이지 로드·제출·버튼별로 **어느 API를 언제** 호출하는지 적는다.  
요청·응답 필드의 상세는 [기능명세상세-백엔드](../기능명세상세-백엔드/README.md) 문서를 따른다.

| 시점 | API 이름 | Method | 용도 |
|------|----------|--------|------|

## 동작

| 사용자 행동 | 프론트 처리 | 성공 시 | 실패 시 |
|-------------|-------------|---------|---------|

## 검증·안내

| 대상 | 규칙·문구 |
|------|-----------|

## 비고

- 
