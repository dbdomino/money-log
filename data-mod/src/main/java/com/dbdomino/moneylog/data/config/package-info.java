/**
 * Persistence-layer configuration (JPA auditing, and related infrastructure).
 *
 * <p>Datasource and JPA properties live in
 * {@code data-mod/src/main/resources/application-postgresql.yml}, not here.
 * Application modules activate them with {@code spring.profiles.active=postgresql}.
 */
package com.dbdomino.moneylog.data.config;
